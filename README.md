# TimbreCanvas

TimbreCanvas is a native, local-first macOS studio for expressive text-to-speech.
The application source is distributed under the MIT License. Model weights and
external inference runtimes are installed separately and remain subject to their
own licenses.

The first supported engine is IndexTTS 2 through an external MLX runtime.

